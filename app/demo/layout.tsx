import type { Metadata } from "next";
export const metadata:Metadata={title:{absolute:"See QANRI at Work | Request a Private Demo"},description:"Request a private QANRI demo and evaluate how assignment, evidence, verification and blockers fit your field operation.",alternates:{canonical:"/demo"}};
export default function DemoLayout({children}:{children:React.ReactNode}){return children}
