import type { Metadata } from "next";
export const metadata:Metadata={title:{absolute:"Terms | QANRI"},description:"QANRI terms information and pilot-stage public-launch boundaries.",alternates:{canonical:"/terms"}};
export default function TermsLayout({children}:{children:React.ReactNode}){return children}
