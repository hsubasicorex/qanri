"use client";
import { useState } from "react";
import type { Locale } from "./i18n";
import { parityCopy } from "./i18n-parity";

export function LocalizedProductTour({locale}:{locale:Locale}){const [active,setActive]=useState(0);const t=parityCopy[locale].tour;const s=t.steps[active];return <section className="tour-section locale-tour" id="product-tour"><div className="section-heading"><p className="micro-label">{t.eyebrow}</p><h2>{t.title}</h2><p>{t.body}</p></div><div className="tour-shell"><div className="tour-tabs" role="tablist" aria-label={t.title}>{t.steps.map((x,i)=><button key={x.label} role="tab" aria-selected={i===active} onClick={()=>setActive(i)}><span>{String(i+1).padStart(2,"0")}</span>{x.label}</button>)}</div><div className="tour-content"><div className="tour-narrative"><p className="tour-overline">{s.screen}</p><h3>{s.title}</h3><p>{s.body}</p><div className="tour-progress"><span style={{width:`${(active+1)*20}%`}}/></div></div><div className="phone-frame" aria-label={`${s.label} interface concept`}><div className="phone-notch"/><div className="phone-status"><span>9:41</span><span>● ● ▰</span></div><div className="phone-head"><span>Q</span><small>{s.screen}</small><i>•••</i></div><div className="phone-greeting">{t.greeting}, Alex<small>{t.attention}</small></div><div className="phone-card"><div><small>{s.meta}</small><strong>{s.task}</strong></div><span>{s.status}</span></div><div className="phone-evidence"><div className="photo-placeholder"><i/><i/><i/></div><div><small>{t.record}</small><b>{s.task}</b><p>{s.body}</p></div></div><button type="button">{s.status}</button></div></div></div></section>}
